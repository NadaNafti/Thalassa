<li class="dropdown">
    <a title="Réservation Voyage Organisé"  data-toggle="dropdown" data-hover="dropdown" class="dropdown-toggle" data-close-others="true" href="javascript:void(0)">
	<i class="clip-tree-2"></i>
	<span class="badge"> {{ reservations|length }}</span>
    </a>
    <ul class="dropdown-menu notifications">
	<li>
	    <span class="dropdown-menu-title">Vous avez {{ reservations|length }} réservation(s)</span>
	</li>
	<li>
	    <div class="drop-down-wrapper">
		<ul>
		    {% for reservation in reservations %}
			<li>
			    <a href="{{ path('back_voyages_organises_reservation_consulter',{'id':reservation.id}) }}">
				<span class="label label-primary"><i class="fa fa-star"></i></span>
				<span class="message">{{ reservation.coordonnees.0 }}</span>
				<span class="time">{{ differenceBetweenDates(reservation.created) }}</span>
			    </a>
			</li>
		    {% endfor %}
		</ul>
	    </div>
	</li>
	<li class="view-all">
	    <a href="{{ path('back_voyages_organises_reservation') }}">
		Voir toutes les réservations <i class="fa fa-arrow-circle-o-right"></i>
	    </a>
	</li>
    </ul>
</li>